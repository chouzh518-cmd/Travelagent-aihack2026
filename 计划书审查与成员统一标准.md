# 差旅助手计划书审查与成员统一标准

审查日期：2026 年 9 月 20 日  
审查材料：9.19meet.docx，标题 On business trip agent

本文包含原计划的问题清单和建议采用的团队统一标准。问题定位使用原文模块标题，不使用未经核验的页码。原计划没有提供成员名单、接口实测记录、实际制度文件、代码或比赛截止时间，因此本文不认定任何外部 API 已可用，不分配具体人员，也不补写接口路径、字段名、认证参数或返回结构。

## 一 审查结论

八个模块已经覆盖从制度导入到审批邮件的主要流程，但尚未形成可共同开发和验收的协议。最先要补齐的是金额语义、制度冲突裁决、未知状态、超标审批边界、跨模块数据关联和邮件发送真实性。否则各模块单独运行成功，也无法证明整体结果正确。

建议先完成一条可复现的主流程：确认员工和差旅需求，获取带依据的制度约束，查询或加载明确标注的交通与酒店数据，计算并校验，展示实际可用的方案，选择方案，生成审批草稿，确认后按明确的真实或演示模式处理发送。预订仍由员工自行完成。

## 二 计划书漏洞与修正要求

下表的“阻断”指未解决前不能通过对应环节的联调验收，不代表不能开展独立开发。

| 等级 | 原文位置与问题 | 对联调或结果的影响 | 修正要求 |
| --- | --- | --- | --- |
| 阻断 | 总原则统一日元；模块 1、2、6 使用“800 元”、800、750，示例未声明币种 | 无法判断制度上限与报价是否能直接比较 | 先确认示例币种；保留制度原始金额及币种。无换算依据不得把原始数值当日元使用 |
| 阻断 | 模块 1 要求无依据的冲突由人确认；模块 2 写死“集团 < 子公司 < 部门” | 部门文件可能在没有授权的情况下覆盖上级制度 | 删除无制度依据的自动优先级；只有原文明确的适用、补充、替代关系才能用于裁决 |
| 阻断 | 总原则要求状态为布尔值，又要求缺失写 null | 未查询、查询失败、无法判断可能被误写成 false 或 true | 为 available、compliant、needs_approval 分别定义 true、false、null 的含义；失败另行说明，禁止用布尔值吞掉错误 |
| 阻断 | 模块 6 淘汰硬性不合规；模块 7、8 又展示需审批和超标说明 | 不清楚哪些方案可提交审批，哪些必须禁止 | 区分不可例外的硬约束、制度明确允许审批的例外、依据不足三种情况 |
| 阻断 | 总目标为“员工说需求即可处理”；模块 3 槽位未包含到达期限等信息 | 无法验证能否准时见客户，也无法可靠选择交通与酒店 | 根据计算需要补充到达期限、返程限制、人数和住宿需求；员工身份及公司适用范围可来自经确认的员工资料 |
| 阻断 | “下周三”没有相对日期解析基准 | 不同时间运行或不同时区会得到不同日期 | 记录需求提交时刻及解释时区，转成绝对日期后让员工确认；测试固定基准时刻 |
| 阻断 | 模块 5 明确列为未验证接口，却要求可订状态 | 查询能返回数据不等于覆盖日本路线、生产可用或可预订 | API 实测分别记录鉴权、环境、目标路线覆盖、价格完整性、可订状态来源；无法验证时保留未知 |
| 阻断 | 只约定 JSON，没有各模块输入输出、关联关系和错误协议 | 对话、报价、方案和邮件无法稳定对应 | 联调前冻结最小数据契约；每个模块注明输入、输出、错误、数据版本、关联对象及责任人 |
| 阻断 | 演示写“邮件真的发出去（或显示发送成功）” | 模拟发送可能被当作真实发送，且重复点击可能重复发信 | 真实和演示模式明确区分；演示只显示演示结果。确认绑定收件人、正文和方案版本，发送支持重复提交保护 |
| 高 | 制度只说明公司隔离，未说明版本、生效时间和适用对象如何参与检索 | 可能引用其他公司、过期版本或不适用职级的条款 | 先按公司和适用条件限制资料范围，再检索；保留制度版本、生效依据与原文位置 |
| 高 | 模块 2 通过向量检索和关键词检索找条款，但没有覆盖检查 | 检索到一条住宿规则不能证明交通、日当和例外规则齐全 | 列出本次行程必须检查的规则类别；任何必需类别缺依据时明确阻断相应结论 |
| 高 | “所有数据都能追溯”与允许模拟数据没有配套标识 | 模拟价格、班次和余量可能被误认为外部接口事实 | 在内部记录、页面、日志、邮件中保留真实或模拟属性；模拟数据追溯到本地文件和版本 |
| 高 | 费用只有加法，没有往返、房晚、人数、税费和日当口径 | 可能漏算或重复计费；缺失项可能被当作零 | 明确计价对象、数量、单位、税费是否包含；未取得必要价格时不得输出完整总价 |
| 高 | 时间只写“在途 + 接驳 + 缓冲” | 无法处理换乘重叠、跨日、去程返程和酒店入住约束 | 各路段保留起止时刻及时间范围；以时间线核对接驳和等待，分别给出去返程总耗时 |
| 高 | 模块 6 用舱等字符串比较；模块 5 接入多个供应方 | 同义标签可能误判，不同产品也可能被错误归并 | 使用经原始样例核对的显式映射；未映射的值保留原文并停止相关自动判断 |
| 高 | Agent 的“比如 5 次”不是确定配置；天气验收仅有一句说明 | 无法一致判定停机条件或天气补查是否合理 | 明确次数、时限、重试计数和停止原因；天气触发依据必须可核验，不得自行判断停运 |
| 高 | 模块 5 包含天气、日程、接驳，但交付物仅列航班、火车、酒店和邮件 | 存在无人交付且会影响计算的依赖 | 将天气、日程、接驳分别列入交付和验收，或明确移出本次演示范围 |
| 高 | 两种排序仅写“价格优先、时间优先” | 不清楚比较单程还是往返、是否包含税费接驳，以及同价怎么处理 | 固定比较范围、排序口径及并列处理规则；说明同一方案是否同时满足两个优先目标 |
| 高 | 邮件面向“多个部门”，但只读取员工职级和部门 | 缺少经验证的收件人来源、审批路线及完成条件 | 提供审批路由表；明确发送草稿或提交申请不代表审批通过；缺收件人时不得发送 |
| 高 | 没有统一集成负责人、阶段门槛、演示用例和失败演练 | 各模块“完成”标准不同，最后集中暴露问题 | 按下文交付包和阶段门槛验收，指定整体集成与演示责任人 |

## 三 全员共同遵循的标准

以下为建议采用的执行规范。既有标识符保持原文拼写；新增业务信息先定义语义，精确字段由接口样例和团队数据契约共同确认。

### 1 范围和责任

1. 每个模块必须有一位最终负责人和一位接收其交付的联调人员。可一人负责多个模块，但每项交付只有一个明确的责任归属。
2. 所有成员使用同一份范围清单、数据契约、测试样例和制度版本，不在各自模块中私自解释公共规则。
3. 天气和日程是否进入首版由团队明确决定；接驳若参与总费用或总时间，就必须有数据和计算责任人。
4. 自动订票、支付、自动修改日期预算、自动批准申请均不属于当前计划已定义的流程。
5. 影响外部系统的操作与一般查询分开处理。邮件发送只能发生在明确预览确认之后。

### 2 数据契约

继续沿用原计划：业务模块之间交换 JSON；字段名使用英文；缺失信息写 null；时间使用带时区偏移的 ISO 8601；方案比较金额采用整数日元。

“业务模块之间交换 JSON”不表示要把制度原始 PDF、Word、网页或邮件 HTML 强制改为 JSON。原始证据保留原格式，结构化元数据通过 JSON 传递。

每份契约必须写清：精确字段名、含义、类型、必填条件、是否允许 null、单位、允许值、产生方、使用方、来源和完整样例。禁止依靠字段大小写转换、结构猜测或模糊名称匹配连接模块。输入不符合契约时返回明确错误，不静默补字段或改类型。

| 跨模块对象 | 必须统一的业务含义 | 产生与使用关系 |
| --- | --- | --- |
| 员工适用信息 | 所属公司、身份、职级、部门，以及经确认的适用条件 | 员工资料提供给制度匹配；只传递任务必要信息 |
| 差旅需求 | 起终点、绝对出返日期、到达要求、人数、住宿需求、偏好、确认状态 | 需求对话交给调度；查询参数据此生成 |
| 制度条款与约束 | 原文、位置、公司、版本、适用范围、约束类型、冲突和缺失情况 | 制度导入及 RAG 交给校验、页面和邮件 |
| 查询结果 | 供应方原始引用、行程或住宿内容、报价范围、币种、有效性、数据真实性、可订依据 | 数据接入交给计算和展示 |
| 方案与计算结果 | 使用哪些查询结果、费用明细、行程时间线、合规依据、未解决问题和版本 | 计算交给方案展示及邮件 |
| 员工选择与确认 | 选择哪个方案及哪个版本、确认时间、确认内容 | 页面交给邮件；需求变化时旧确认失效 |
| 邮件和发送结果 | 收件人来源、预览内容、确认关联、真实或演示模式、发送结果证据 | 邮件模块交给页面和日志 |
| 执行记录 | 本次执行的关联信息、工具调用、错误、重试、停止原因和耗时 | 所有模块交给统一日志与演示回放 |

这些是语义清单，不是现有 API 的字段声明，也不是已冻结的 JSON Schema。没有实际响应支撑时不补写供应方标识符。

### 3 来源和状态

source 与 queried_at 沿用原文精确拼写。source 必须足以定位供应方记录或模拟文件；queried_at 表示本次查询时刻，不代表供应方数据更新时刻，也不自动证明报价仍有效。制度和计算结果还要能追溯到实际引用的条款或输入记录。

建议统一采用以下三值语义，并在数据契约中声明允许 null：

| 字段 | true | false | null |
| --- | --- | --- | --- |
| available | 有明确依据表明在查询时可订 | 有明确依据表明在查询时不可订 | 未验证、查询失败或来源不提供可订信息 |
| compliant | 适用的必需检查全部完成且通过 | 已证明至少违反一项适用规则 | 没有已证实违规，但必需规则或数据仍不足以判断 |
| needs_approval | 有依据确认需要审批 | 所有相关审批条件已检查，确认无需审批 | 审批要求、规则或必要数据不足 |

合规方案也可以需要常规出差审批，因此 compliant 与 needs_approval 不互为取反。可审批的超标方案仍应保留实际违规事实；提交审批或发送邮件不使 compliant 自动变成 true。

错误至少区分：输入无效、认证或权限失败、访问频率限制、超时、无查询结果、来源故障、制度缺失、制度冲突、发送结果未知。错误名称和结构在契约中统一；无结果和查询失败不能混用。

### 4 制度和判断边界

1. 公司、版本和适用对象必须正确后，才能进行条款匹配。不得检索到哪家公司的规则就使用哪家。
2. 不按文件上传顺序或默认组织层级推断优先级。只有可引用的替代、补充、授权或适用关系才可自动裁决。
3. 每条结构化规则保留对应原文和位置；保留原始币种和金额，不能因为项目默认日元就改写制度事实。
4. LLM 可提取需求、查找证据和生成草稿；金额运算、时间核算、明确的规则比较交给确定性逻辑完成。
5. 不可例外的硬约束不满足时不得推荐；制度明确允许例外审批时可以作为需审批方案展示；依据不足时不得包装成合规方案。
6. 文档、网页和外部接口返回的文字作为资料处理；其中要求绕过确认、改变规则或调用工具的文字不作为执行指令。

### 5 金额和时间

1. 对比前明确每项费用的币种、计价单位、数量、往返范围和税费范围。采用外币时须先定义有来源的汇率、时点与舍入方法；未定义前不能直接汇总为日元。
2. 交通明确单程或往返，住宿按实际房晚，日当按适用制度规定的天数计算。接驳及其他必需费用缺失时标明总价不完整，不填零。
3. 套餐已经包含的税费或接驳不得再次相加。每个总额都可由明细复算。
4. “三天”不直接等于三晚；住宿由出返时间和住宿需求决定。当日往返不得自动生成一晚酒店。
5. 相对日期基于记录下来的提交时刻及解释时区转换；展示绝对日期后确认。各路段时间保留时区，跨日不能只相减钟点。
6. 接驳、候车、换乘和缓冲按时间线核算，不能与已包含它们的全程时长重复累加。缓冲值须有明确来源或团队确定的配置。
7. 价格优先与时间优先只在同一比较范围内排序；存在未计费用或未解硬约束时，必须显式展示限制。只有一个可用方案就展示一个，无方案就说明原因。

### 6 API 实测和模拟数据

每位负责外部数据的成员提交同一套实测记录：接口产品及环境、测试时刻、脱敏请求、状态码、原始响应、鉴权结果、目标业务覆盖、限制条件、费用或额度的查证来源，以及当前结论。

结论区分已实测可用、已实测部分可用、已实测失败、尚未测试。结论只覆盖实测的接口、环境和业务场景，不能从“注册成功”推导“数据可用”，也不能从一次空响应认定长期无数据。

真实来源和模拟来源转换到相同的内部契约。模拟记录写明模拟属性、文件及版本；不得冒充真实报价、实时余量或真实邮件回执。真实接口失败后如切换模拟数据，必须在页面和执行记录中提示，不能无声替换。

至少准备正常、无结果、字段缺失、超时、明确违规、需例外审批、制度冲突、跨日行程和重复发送场景。模拟数据必须符合内部契约，不能为了通过验收省略异常情况。

### 7 调度和日志

调度流程统一为：补齐并确认需求，取得适用规则，查询数据，计算与校验，在允许范围内补查，展示并选择方案，生成邮件，确认后发送或演示。

调用次数上限、整体时限、单次超时、重试次数与计数范围须作为明确配置，不能保留“比如 5 次”。并行调用、重试是否计入上限也要写清。停止时返回已完成内容、未解决内容和停止原因。

补查记录只需可核验的业务原因、使用的输入、调用的工具、结果摘要和后续动作，不要求暴露模型内部推理。日志贯通一次需求到方案及邮件的关联关系，且不记录密码、访问令牌或不必要的个人资料。

### 8 页面和邮件

页面、日志和邮件对同一个方案使用同一个版本的金额、时间和依据。变更日期、报价、规则、收件人或正文后，原预览确认不继续生效。

发送前展示真实收件人、主题、完整正文、金额和方案版本。演示发送明确显示未向外部发送；真实发送只按接口返回证据报告状态，不把接口接受请求等同于送达或审批通过。

发送超时后若结果未知，先查询或核对结果，不盲目再次发送。重复点击不能产生重复邮件。占位预订链接明确标注且不能表现为有效预订入口。

## 四 按模块统一交付和验收

原计划已有的路径保持原样，不把尚未定义的模块放进猜测的目录。

| 模块 | 原计划中的交付物 | 联调时必须补充的验收 |
| --- | --- | --- |
| 1 制度导入 | data/policies/ 与 storage/chroma/ | 原文可回查；公司隔离；版本与位置保留；无法提取文本时显式报错；原始文件不被覆盖 |
| 2 制度 RAG | core/rule_matcher.py 与约束清单 JSON | 正确适用对象；逐条证据；冲突不擅自裁决；缺少必需条款时不能给出完整合规结论 |
| 3 需求对话 | core/intent_parser.py 与 prompts/ask_missing.txt | 相对日期可复现；缺失信息会追问；绝对行程经确认；修改需求使相关下游结果失效 |
| 4 Agent 调度 | core/planner.py 与执行日志 | 补查原因有依据；达到确定的限制时停止；不自行改变日期预算；错误可传递到页面 |
| 5 数据接入 | tools/flight_api.py、tools/train_api.py、tools/hotel_api.py、tools/email_api.py | 原始响应及映射可审查；真实模拟契约一致；零结果和失败分离；天气、日程及接驳另补范围和交付责任 |
| 6 计算与校验 | core/calculator.py，原文另提到可放在 B 的工具中 | 团队选定唯一计算实现；明细可复算；币种、税费、房晚与跨日正确；三种合规处理边界明确 |
| 7 方案展示 | app.py 或 templates/plan_compare.html | 团队选定实现；能处理零个、一个、多个方案；真实模拟可辨；选择传递明确方案及版本 |
| 8 审批邮件 | core/emailer.py、templates/approval_email.html 与发送日志 | 收件人来源可查；确认绑定内容；演示不冒充实发；重复发送保护；未知结果不误报成功 |

每个模块都交付：运行说明、输入输出契约、正常与异常样例、验证步骤及结果、外部依赖说明、当前未完成项。涉及配置时记录精确配置项与获取方式，不提交密钥。仅交代码文件不算完成。

成员对公共契约的变更必须同步通知产生方、使用方和集成负责人。变更记录包含改动内容、原因、影响模块、样例更新和迁移方式；修改后使用同一组端到端样例重新验收。具体仓库流程和分支名称以实际项目规定为准。

## 五 团队统一验收用例

| 场景 | 必须观察到的结果 |
| --- | --- |
| 员工当天往返且信息完整 | 不强制查询酒店；往返交通与接驳按明细核算 |
| 输入“下周三去大阪当天回”且缺出发地 | 追问出发地，按固定基准解释日期，并在查询前确认必要信息 |
| 制度上限使用未确认的“元” | 阻断与日元报价的数值比较，不擅自解释币种 |
| 两份制度相互冲突且没有替代依据 | 显示冲突原文，等待人工确认，不按组织层级猜测 |
| 制度明确禁止的方案 | 不作为可推荐方案展示，并保留淘汰原因 |
| 制度允许例外审批的超标方案 | 展示违规事实和例外依据，标记需审批，不宣称已合规或已获批准 |
| 必需价格或规则缺失 | 显示缺失，相关结论保持未知，不把缺失价格当零 |
| API 超时或返回空结果 | 分别展示失败或无结果；切模拟时提示；不伪造第二个方案 |
| 不同公司有相似条款 | 只使用当前员工所属公司的适用条款 |
| 行程跨日或报价包含税费接驳 | 时间计算正确，不漏算或重复计算费用 |
| 员工选定后修改需求或报价更新 | 旧方案及旧确认不再用于直接发送，重新计算并确认 |
| 连续点击发送或发送响应超时 | 不重复发信；无确定结果时显示待核对，不显示发送成功 |
| 演示邮件流程 | 全程可辨认未向外部发送，页面和日志不出现真实发送成功结论 |

以上用例的具体输入、预期金额、时刻和规则须由实际制度与确认后的样例补齐，不能在没有证据时编造“正确答案”。

## 六 推进顺序和需要补充的材料

按验收门槛推进，不在缺少截止时间和成员投入信息时编造日期排期。

1. 范围与责任确认：确定主流程、扩展功能、模块负责人和集成负责人。
2. 数据契约冻结：收齐制度与接口样例，解决币种、状态、审批边界、字段和关联关系。
3. 模拟全链路通过：使用统一样例从需求走到邮件预览和明确标注的演示发送。
4. 已验证接口替换：逐个接入真实来源，保持内部契约不变，复跑正常与异常场景。
5. 演示验收：固定制度、数据版本、运行步骤和备用演示方式，完成完整流程及失败场景演练。

需要团队补充的材料：

- 成员名单、模块分工、整体集成负责人、演示截止时间。
- 已实测 API 的产品名称、环境和脱敏请求响应，尤其是原计划中的 OrcaRouter 调用证据。
- 本次演示使用的制度原文件及版本，确认“800 元”的具体币种。
- 员工资料的实际样例与公司适用关系；不要发送真实敏感资料。
- 审批收件人来源、部门路由、例外审批规则，以及邮件采用真实还是演示发送。
- 天气、日程、接驳本次必须实现到什么程度。

材料收齐后，才能把本文语义规范落实为准确的数据字段、请求配置和带确定预期值的联调用例。
